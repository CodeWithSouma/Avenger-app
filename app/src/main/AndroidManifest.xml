<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codewithsouma.activitylifecycle">

    <application
        android:allowBackup="true"
        android:icon="@drawable/avenger"
        android:label="@string/new_app_name"
        android:roundIcon="@drawable/avenger"
        android:supportsRtl="true"
        android:theme="@style/Theme.ActivityLifecycle">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AvengersActivity">
        </activity>
    </application>

</manifest>